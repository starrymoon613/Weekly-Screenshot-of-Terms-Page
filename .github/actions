
name: Run Screenshot

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run screenshot action
        uses: ./.github/actions/cv-terms-screenshot
